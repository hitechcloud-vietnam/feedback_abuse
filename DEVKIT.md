# feedback_abuse — HostBill Module Devkit

> **Generated by** `.agents/.mcp/gen_hostbill_round4.py` on 2026-06-11.
> **Source:** `module_dev_hostbill\Other\feedback_abuse`
> **Base class:** `OtherModule`
> **Subtype:** Other (feedback / abuse / phishing / malware / botnet / spam / domain_abuse / network_abuse)
> **Schema version:** 1.0.0 (10 sections, round-3 compatible)

---

## 1. Header

- **Name:** `feedback_abuse`
- **Base class:** `OtherModule` (extends `Module`)
- **Subtype:** Other — public-facing form intake (no provisioning, no payments)
- **Author:** AI-generated under `.agents/PROTOCOL.md` Round-6 workflow
- **Source:** `module_dev_hostbill\Other\feedback_abuse`

## 2. Configuration

- `enabled` (CHECK): Master switch — when off, the embed widget and JSON API return 503.
- `enabled_types` (CHECKLIST): Which report types appear on the public widget and admin form.
  Default: `feedback,phishing,malware,botnet,spam,domain_abuse,network_abuse`
- `allow_attachments` (CHECK): When off, the upload field is hidden on every form.
- `allowed_extensions` (INPUT): Comma-separated extension allowlist (no dots).
  Default: `doc,docx,xls,xlsx,pdf,jpg,jpeg,gif,png,bmp,ico,zip,rar,txt,csv`
- `max_file_size_mb` (INPUT): Per-file cap. Default `10`. PHP `upload_max_filesize` / `post_max_size` must be ≥ this.
- `storage_path` (INPUT): Absolute path on disk. Created if missing. Files stored under `YYYY/MM/<sha256>.<ext>`.
- `rate_limit_per_hour` (INPUT): Anti-spam throttle per IP per hour. `0` disables. Default `10`.
- `require_captcha` (CHECK): When on, public submissions must pass a CAPTCHA challenge.
- `notify_admin_email` (INPUT): Comma-separated recipients for new-report emails. Empty = disabled.
- `embed_hmac_secret` (PASSWORD): Shared secret for signing 3rd-party embed tokens. Rotate quarterly.
- `embed_token_ttl` (INPUT): How long an issued embed token is valid. Default `86400` (1 day).
- `public_api_enabled` (CHECK): When off, the JSON `/api/feedback_abuse/submit` returns 404.
- `client_area_enabled` (CHECK): Show report list in client portal for logged-in clients.
- `auto_close_after_days` (INPUT): Auto-close `new` reports after N days. `0` disables. Default `30`.
- `language_default` (SELECT): Default embed-widget language when the visitor has no `Accept-Language` preference. `english` | `vietnamese`.

## 3. API client

- **Public API class:** `api/class.feedback_abuse_apiroutes.php`
- **Routes manifest:** `api/feedback_abuse_apiroutes.json`
- **Submission service:** `lib/class.report_service.php` (used by widget, user controller, and JSON API)
- **External libs:** none (no Composer dependency)

## 4. Lifecycle methods

Public methods on the main class `feedback_abuse`:

- `install()`            — runs `install.sql` split by `######` (idempotent)
- `upgrade()`            — re-runs `install()` (idempotent)
- `uninstall()`          — drops the 6 tables
- `ensureStoragePath()`  — creates the storage directory + .htaccess + index.html
- `boolConfig($k, $d)`   — typed accessors
- `intConfig($k, $d)`
- `strConfig($k, $d)`
- `enabledTypes()`       — returns the enabled-types list
- `allowedExtensions()`  — returns the lower-cased ext allowlist
- `maxFileSizeBytes()`   — returns the byte cap
- `getMeta($k, $d)`      — read a module-level meta value
- `setMeta($k, $v)`      — write a module-level meta value (admin-controller mediated)

Event hooks (Observer):

- `after_report($params)` in `event/class.feedback_abuse_handle.php` — email + audit.
- `after_status_change($params)` in `event/class.feedback_abuse_handle.php` — audit.
- Producers emit both events through `HBEventManager::notify(...)`.

Cron:

- `cron/class.feedback_abuse_cron.php::run()` — daily cleanup.

## 5. Admin UI

- **Main class:** `class.feedback_abuse.php`
- **Base class:** `OtherModule`
- **Subtype:** Other
- **Widget:** yes (`widgets/widget.feedback_form.php`)
- **Widget templates:** yes (inline + embed)
- **Admin folder:** yes (`admin/`)
- **Cron folder:** yes (`cron/`)
- **Event observer:** yes (`event/`)
- **ORM folder:** yes — 6 Eloquent models in `orm/`
- **Eloquent models:** 6 (`Report`, `Attachment`, `Note`, `Rate`, `Token`, `Audit`)
- **lib folder:** yes (4 helpers)
- **install.sql:** yes (6 tables, `######` separator)
- **user/templates (anti-pattern):** clean — `user/template.tpl` is allowed for OtherModule per CLAUDE.md §7.29.5

## 6. Client UI

- **Widget file:** **YES** — `widgets/widget.feedback_form.php` (the embeddable form).
- **Widget templates:** `widget_templates/widget_feedback_form_inline.tpl` (HostBill-hosted) and `widget_templates/widget_feedback_form_embed.tpl` (3rd-party iframe).
- **User controller:** `user/class.feedback_abuse_controller.php` for the client-portal "Extras" menu entry.
- **User template:** `user/template.tpl`.
- **Anti-pattern check:** clean — no `user/templates/` directory (CLAUDE.md §-1 widget rule respected).

## 7. Hooks

Detected Observer hooks:

- `feedback_abuse_handle::after_report($params)` handles new report notifications.
- `feedback_abuse_handle::after_status_change($params)` handles status-change auditing.

`HBEventManager` listeners (emitted through `HBEventManager::notify`):

- `after_report` — payload: `module`, `report_id`, `public_id`, `type`, `email`.
- `after_status_change` — payload: `module`, `report_id`, `admin_id`, `from`, `to`, `ip`.

HostBill discovers the public event methods and rebuilds `hb_event_handles`
when the module is activated or upgraded.

## 8. Database schema

- **install.sql:** present, uses `######` separator, 6 tables.
- **ORM folder:** 6 Eloquent-based models in `orm/` (per memory [HostBill ORM pattern]).
- **Tables (all `hb_` prefix):**
  1. `hb_feedback_abuse_reports` — main report rows (with `public_id` tracking)
  2. `hb_feedback_abuse_attachments` — uploaded files (sha256-addressed)
  3. `hb_feedback_abuse_notes` — internal admin notes
  4. `hb_feedback_abuse_rate` — sliding-window per-IP rate-limit cache
  5. `hb_feedback_abuse_tokens` — embed tokens (HMAC-signed, revocable)
  6. `hb_feedback_abuse_audit` — append-only audit log

## 9. Test cases

- `php -l` clean on every PHP file in the module (16/16 pass).
- Manual review of generated DEVKIT.md against the source module.
- For Other modules with embed widgets: verify the iframe template's CSRF, file-size hint, allowed-extensions hint, and the `postMessage` resize handler.
- For modules with Eloquent models: verify the namespace `Other\feedback_abuse\ORM\…` matches the Eloquent `HBLoader::LoadModel(...)` lookups in controllers.
- For modules with `install.sql`: verify the `######` separator is present and every `CREATE TABLE` uses `IF NOT EXISTS`.

## 10. Reference

- **Source module:** `module_dev_hostbill/Other/feedback_abuse`
- **Workflow used:** `.agents/PROTOCOL.md` Round-6 single-entry-point
- **Related memory:** `hostbill-orm-pattern.md`, `hostbill-typed-property-pitfall.md`
- **Reference modules read (NOT copied):**
  - `module_dev_hostbill/Other/referral_program` — `OtherModule` + `Observer` + `Widget` pattern
  - `module_dev_hostbill/Other/affiliate` — Eloquent + `install.sql` `######` pattern
  - `Core_examp_hostbill/Other/adminnotes` — admin widget + ORM pattern
  - `Core_examp_hostbill/Other/billingalerts` — `user/` directory pattern (valid for OtherModule)
  - `Core_examp_hostbill/Other/disqus` — `clientwidget_` ajax pattern
  - `Core_examp_hostbill/Other/ai_ticket_spamfilter` — Observer-style lifecycle hook

---

_Devkit schema: 10 sections. Generator: AI / `.agents/PROTOCOL.md` Round-6. Emit date: 2026-06-11._
